// TODO


// Edge operation
/*
export const updateEdgeLabel = (event, selectedMetaAction, selectedEdgeid) => {
    const updatedMetaActionArray = metaActionArray.map(metaAction => (metaAction.id === selectedMetaAction.name ? 
        { ...metaAction, flow: selectedMetaAction.flow.map(nodeOrEdge => nodeOrEdge.id === selectedEdgeid ? { ...nodeOrEdge, label: event.target.value } : nodeOrEdge) } :
         metaAction))

    setMetaActionArray(updatedMetaActionArray)        
}
*/

